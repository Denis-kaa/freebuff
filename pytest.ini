[pytest]
markers =
    slow: marks tests as slow (real-subprocess/network integration; deselect with '-m "not slow"')
    xdist_group: run tests in the same group on the same xdist worker (serialize shared-registry files)
