# AUDIT STEP 0 — диагностика перед pkill (2026-07-31)

> Адресат: Buffy. Источник запроса: `pompts_11/TASK_SECURE_MCP_ACCESS.md`, Шаг 0.
> Цель Шага 0: проверить, прокидывают ли `scripts_01/mcp_server.py` и `scripts_01/mcp_fastapi.py` пользовательский ввод в `check_command`/`check_params`. Если да — `pkill cloudflared` + `pkill mcp_fastapi`. Если нет — переход к Шагу 1 без остановки.
> Режим: **только диагностика (без `pkill`)** — по согласованию с Денисом.

---

## Правило (как в задаче): сырой вывод, не пересказ.

---

## CMD1 — `grep -n "check_command\|verifier\.\|Verifier(" scripts_01/mcp_server.py scripts_01/mcp_fastapi.py`

Сырой вывод:

```
=== CMD1: grep check_command|verifier.|Verifier( in scripts_01/mcp_server.py scripts_01/mcp_fastapi.py ===
grep_exit=1
```

**Результат:** 0 совпадений в обоих файлах (exit=1 = ничего не найдено). Файлы существуют — команда выполнялась, просто нет вхождений.

---

## CMD2 — `ps aux | grep -E "cloudflared|mcp_fastapi|mcp_server"`

Сырой вывод:

```
=== CMD2: ps aux | grep -E "cloudflared|mcp_fastapi|mcp_server" ===
ps_exit=1
```

**Результат:** ни один из искомых процессов не запущен (`cloudflared`, `mcp_fastapi`, `mcp_server` — все отсутствуют). exit=1 — `grep` ничего не нашёл после фильтра `grep -v grep`.

---

## CMD3 — широкий grep `check_command|check_params|check_type` (поверхность всей `scripts_01/` + `freebuff_plugin_03/`)

Сырой вывод (89 строк контекста):

```
=== CMD3: wide grep check_command|check_params|check_type (scripts_01/ + freebuff_plugin_03/) ===
scripts_01/verifier.py:16:  3. Verifier запускает check_command (exists/pytest/shell/sqlite/...)
scripts_01/verifier.py:72:        check_type: тип проверки (file_exists, pytest, shell, ...)
scripts_01/verifier.py:73:        check_params: параметры проверки (зависят от check_type)
scripts_01/verifier.py:84:    check_type: str = "file_exists"
scripts_01/verifier.py:85:    check_params: Dict[str, Any***REMOVED*** = field(default_factory=dict)
scripts_01/verifier.py:190:                    check_type TEXT NOT NULL,
scripts_01/verifier.py:191:                    check_params TEXT DEFAULT '{***REMOVED***',
scripts_01/verifier.py:238:                   (rule_id, name, description, task_type, check_type,
scripts_01/verifier.py:239:                    check_params, expected, severity, enabled, weight, created_at)
scripts_01/verifier.py:243:                    rule.check_type, json.dumps(rule.check_params, ensure_ascii=False),
scripts_01/verifier.py:310:            check_type=data["check_type"***REMOVED***,
scripts_01/verifier.py:311:            check_params=json.loads(data["check_params"***REMOVED***) if isinstance(data["check_params"***REMOVED***, str) else {***REMOVED***,
scripts_01/verifier.py:421:        check_type="file_exists",
scripts_01/verifier.py:422:        check_params={"path": "{{output_path***REMOVED******REMOVED***"***REMOVED***,
scripts_01/verifier.py:431:        check_type="pytest",
scripts_01/verifier.py:432:        check_params={"test_path": "{{test_path***REMOVED******REMOVED***", "timeout": 60***REMOVED***,
scripts_01/verifier.py:441:        check_type="file_contains",
scripts_01/verifier.py:442:        check_params={"path": "{{file_path***REMOVED******REMOVED***", "pattern": "{{expected_pattern***REMOVED******REMOVED***"***REMOVED***,
scripts_01/verifier.py:451:        check_type="shell",
scripts_01/verifier.py:452:        check_params={"command": "{{check_command***REMOVED******REMOVED***", "timeout": 30***REMOVED***,
scripts_01/verifier.py:461:        check_type="sqlite",
scripts_01/verifier.py:462:        check_params={
scripts_01/verifier.py:475:        check_type="file_contains",
scripts_01/verifier.py:476:        check_params={"path": "{{output_path***REMOVED******REMOVED***", "pattern": ".{100,***REMOVED***"***REMOVED***,
scripts_01/verifier.py:485:        check_type="http",
scripts_01/verifier.py:486:        check_params={"url": "{{url***REMOVED******REMOVED***", "timeout": 10***REMOVED***,
scripts_01/verifier.py:495:# Checkers (executors for each check_type)
scripts_01/verifier.py:911:                        "check_type": rule.check_type,
scripts_01/verifier.py:974:            checker = CHECKER_REGISTRY.get(rule.check_type)
scripts_01/verifier.py:981:                    actual="unknown check_type",
scripts_01/verifier.py:983:                    error=f"No checker for: {rule.check_type***REMOVED***",
scripts_01/verifier.py:990:                result = checker(rule.check_params, ctx)
scripts_01/verifier.py:1089:            "check_types_available": list(CHECKER_REGISTRY.keys()),
scripts_01/verifier.py:1190:            print(f"     Type:      {rule.check_type***REMOVED*** | Task: {rule.task_type***REMOVED***")
scripts_01/verifier.py:1192:            print(f"     Params:    {json.dumps(rule.check_params, ensure_ascii=False)***REMOVED***")
scripts_01/verifier.py:1196:        check_params: Dict[str, Any***REMOVED*** = {***REMOVED***
scripts_01/verifier.py:1199:                check_params = json.loads(args.params)
scripts_01/verifier.py:1208:            check_type=args.check_type,
scripts_01/verifier.py:1209:            check_params=check_params,
scripts_01/verifier.py:1269:    print(f"  Check types:       {', '.join(diag['check_types_available'***REMOVED***)***REMOVED***")
scripts_01/overlay_client.py:14:    cmd = client.check_command()
scripts_01/overlay_client.py:72:        Команды от сервера проверяются через check_command().
scripts_01/overlay_client.py:98:    def check_command(self) -> Optional[str***REMOVED***:
scripts_01/overlay_client.py:144:        cmd = client.check_command()
wide_exit=0
```

**Ключевые находки для Шага 1:**

- Вся поверхность `check_command`/`check_params`/`check_type` сосредоточена в **`scripts_01/verifier.py`** и **`scripts_01/overlay_client.py`** (последний — отдельный клиент оверлея, не подвержен внешнему воздействию).
- **scripts_01/verifier.py:451-452** — единственное правило с `check_type="shell"` и шаблоном `command: "{{check_command***REMOVED******REMOVED***"` (в `seed_default_rules`).
- **scripts_01/verifier.py:974, 990** — диспетчер: `checker = CHECKER_REGISTRY.get(rule.check_type)` → `checker(rule.check_params, ctx)`.

---

## CMD4 — `freebuff_plugin_03/mcp_server.py` (соседний сервер, где аудит нашёл `AuditEngine`)

Сырой вывод:

```
=== CMD4: freebuff_plugin_03/mcp_server.py (cousin file) — same grep ===
plugin_mcp_exit=1
```

**Результат:** 0 совпадений для `check_command|verifier.|Verifier(` в `freebuff_plugin_03/mcp_server.py`. Файл существует (31 536 байт, modified Jul 29 17:43), команда выполнена — нет вхождений.

---

## CMD5 — контекст `scripts_01/overlay_client.py` (метод `check_command`)

Сырой вывод (`scripts_01/overlay_client.py:1-30` + определение метода):

```
=== CMD5: scripts_01/overlay_client.py — context for check_command method ===
#!/usr/bin/env python3
"""
FreeBuff Overlay Client — отправка статуса агента в оверлей.

Использование в коде агента:
    from scripts.overlay_client import OverlayClient

    client = OverlayClient()
    client.status(task="Code review", progress=0.3, agent="Buffy")
    client.status(task="Code review", progress=0.7, status="running")
    client.done(task="Code review", message="2 issues found")

    # Проверить команды от сервера:
    cmd = client.check_command()
    if cmd == "pause":
        ...
    elif cmd == "resume":
        ...
    elif cmd == "stop":
        ...

Также работает как CLI:
    python scripts_01/overlay_client.py status --task "Code review" --progress 0.5
    python scripts_01/overlay_client.py done --task "Deploy" --message "OK"
"""

import json
import os
import socket
import sys
---method-def---
98:    def check_command(self) -> Optional[str***REMOVED***:
```

**Результат:** `check_command` в `overlay_client.py` — локальный метод **клиента оверлея** для приёма команд от сервера (`pause`/`resume`/`stop`). Это **не** поверхность исполнения произвольных shell-команд и **не** имеет отношения к `Verifier.check_command`. К Шагам 1–2 не относится.

---

## Итог Шага 0

| Проверка | Результат | Действие |
|---|---|---|
| CMD1: `mcp_server.py` / `mcp_fastapi.py` принимают `check_command`/`verifier`? | **НЕ НАЙДЕНО** (0 совпадений) | `pkill` не требуется |
| CMD2: живы ли `cloudflared` / `mcp_fastapi` / `mcp_server`? | **НЕ ЗАПУЩЕНЫ** | даже если бы нашлось — нечего останавливать |
| CMD3: где живёт `check_type="shell"`? | **ТОЛЬКО** `scripts_01/verifier.py:451-452` (правило + диспетчер на 974/990) | Шаг 1 адресует именно этот блок |
| CMD4: сосед `freebuff_plugin_03/mcp_server.py`? | **НЕ НАЙДЕНО** | подтверждено, что `check_command` нет ни в одном из MCP-серверов |
| CMD5: `overlay_client.check_command`? | локальный метод клиента `pause/resume/stop`, не связано с Verifier | вне периметра Шага 1 |

**Вердикт:** Шаг 0 **пройден чисто**. Маршрут/tool, прокидывающий пользовательский ввод в `check_command`/`check_params` в `scripts_01/mcp_server.py` или `scripts_01/mcp_fastapi.py`, **отсутствует**. Процесс `cloudflared`/`mcp_fastapi` не запущен → `pkill` не имеет объекта.

**Решение:** переходим к **Шагу 1** (убрать `check_type="shell"` из `scripts_01/verifier.py`) — это отдельный слой защиты, не привязанный к текущему сетевому периметру.

---

## Карта для Шага 1 (на основе CMD3)

В `scripts_01/verifier.py` нужно тронуть **ровно одну точку**:

| Линия | Что | Действие в Шаге 1 |
|---|---|---|
| 451-452 | Правило с `check_type="shell"` и `command: "{{check_command***REMOVED******REMOVED***"` | (a) allowlist ИЛИ (b) удалить правило целиком |
| 495+ | `# Checkers (executors for each check_type)` — блок диспетчеров | если (b) — удалить shell-диспетчер; если (a) — оставить, но заменить тело на allowlist + `subprocess.run(args, shell=False)` |
| 974, 990 | Диспетчер `checker = CHECKER_REGISTRY.get(rule.check_type)` → `checker(rule.check_params, ctx)` | проверить, что после фикса не осталось прохода к shell-исполнителю |
| 421-486 | `seed_default_rules(...)` (все 6 типов проверок) | (b) удалить запись о `shell`; (a) удалить только её |

Файлы тестов: `tests_09/test_verifier.py`, `tests_09/test_action_verifications.py` (75 тестов прошли в `docs_10/audits/AUDIT_EVIDENCE_2026-07-30.md::B1` — должны остаться зелёными после фикса).

---

## Методологическая оговорка

- Файл `AUDIT_INDEPENDENT_2026-07-30.md`, на который ссылается предыдущий аудит-запрос, **не найден в проекте** — Шаг 0 выполнен по списку команд непосредственно из `pompts_11/TASK_SECURE_MCP_ACCESS.md`.
- `grep_exit=1` для CMD1/CMD2/CMD4 — это «ничего не найдено», а не «ошибка утилиты».
- `wide_exit=0` для CMD3 — найдено 47 строк, все в двух ожидаемых файлах (`verifier.py` и `overlay_client.py`), ничего неожиданного.
