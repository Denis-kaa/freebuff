# Git-Recovery Audit: `***REMOVED***` (2026-08-30)

## Метод

Однопроходный скрипт `scripts_01/recover_clean_from_git.py`:
1. нашёл все tracked-файлы с маркером в рабочем дереве;
2. один проход `git log --all --raw` построил карту path → (date, commit, blob);
3. один проход `git cat-file --batch` проверил до 200 последних blob на каждый путь и выгрузил первый без маркера.

Рабочее дерево и Git-история не изменялись. Всё записано в `.recovery_git_clean/`.

## Результат

| Метрика | Значение |
|---|---|
| Повреждено tracked-файлов | 1232 |
| Чистые версии найдены в Git | **5** |
| Чистых версий НЕТ в Git | **1227** |

## Вывод

Git-история **не является** источником восстановления: маркер сидит в самих коммитах с рождения (коррупция вошла в историю до первого коммита с этими файлами). Только 5 файла имеют чистую версию в истории:

- `AGENTS.md` ← `1c989258d4`
- `BUFFY_PROJECT.md` ← `7ef488f26c`
- `TASK.md` ← `a79a1fb6c6`
- `docs_10/core/ARCHITECTURAL_DEBT.md` ← `7ef488f26c`
- `projects_17/public_request_parser/STEPS.md` ← `64ded1e153`

## Что осталось восстанавливать не из Git

| Тип | Файлов | Источник |
|---|---|---|
| `.py` (без clean в Git) | 535 | AST-guided bracket repair (автоматический) |
| `.md` (без clean в Git) | 536 | Text-guided repair (скобки, markdown-таблицы) |
| `.json` (без clean в Git) | 49 | JSON-guided repair + re-parse |
| прочее | 107 | ручная проверка |

## Следующий шаг

Автоматический ремонт по типам: `.py` через AST-guided bracket repair, `.json` через re-parse, `.md` через контекстные правила. Каждый файл валидируется после ремонта (ast.parse / json.loads) перед записью.
