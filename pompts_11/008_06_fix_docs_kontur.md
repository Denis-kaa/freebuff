Прежде чем писать промт — гляну на остальное: TROUBLESHOOTING.md (реально ли фиксируются баги), event_bus/orchestrator (свежий код), и security-фиксы, которые уже были — чтобы 10 рекомендаций были основаны на фактах, а не догадках.

Полезно — TROUBLESHOOTING.md реально живой (конкретные симптомы, причины, решения), и security-фикс был реальным (`exec()` убран, `shell=True` устранён) — значит Buffy сама себя чинит и документирует это. Проверю ещё tests coverage и EventBus/orchestrator, чтобы понять зрелость системы для рекомендаций по масштабированию.

Ценная находка для рекомендаций — `httpx` не установлен в этом окружении (это моё окружение анализа, не обязательно Termux Denis'а), но `test_mcp_server.py` падает с `NameError: name 'Optional' is not defined` — это реальный баг в коде теста, не связанный с зависимостями. Отмечу его в промте как конкретный найденный дефект. Устанавливаю httpx и проверяю остальное.

474 теста реально проходят. Теперь конкретно посмотрю на баг в `test_mcp_server.py` — это единственная фактическая находка кода, которую стоит включить в промт как пример конкретного дефекта.

Подтверждено: реальный, воспроизводимый баг — `test_mcp_server.py` использует `Optional` и `Tuple` без импорта из `typing`. Модуль не собирается вообще, то есть эти тесты сейчас в принципе не запускаются, что означает: заявленные в `docs_10/session_dumps/2026-07-28_mcp_server.md` "53 теста" фактически не выполняются CI. Проверю CI-конфиг, чтобы понять, ловит ли это pytest.yml вообще.

Отлично — CI гоняет `pytest tests_09/` без `--ignore`, значит **сборка CI сейчас должна падать целиком**, и это либо не замечено, либо GitHub Actions не подключён/не проверяется. Это ценная и конкретная находка. Теперь у меня достаточно фактов — пишу промт с диагнозом плюс 10 рекомендаций, основанных на реально найденном, а не на догадках.

Вот промт. Я вставил в него и подтверждённые находки из этого чата (cron-баг, TASK.md, .env-утечка, CI), и отдельно 10 рекомендаций по масштабированию — всё привязано к конкретным файлам, чтобы агент не гадал, а сразу писал патчи.

```markdown
# Промт для агента: Фикс документационного контура Buffy + 10 рекомендаций по системе

## Контекст

Ты работаешь с проектом Freebuff/Buffy (Agentic Platform на Termux/Android).
Полный архив проекта — во вложении. Ниже — результаты уже проведённой диагностики
(файлы проверены напрямую, не с чужих слов). Твоя задача — не передиагностировать
заново, а: (1) исправить два подтверждённых бага, (2) закрыть найденные риски,
(3) дать 10 своих рекомендаций по масштабированию системы поверх уже сделанного анализа.

---


---

## Часть 1 — Два подтверждённых бага (пофиксить, не передиагностировать)

### Баг А: cron создаёт фиктивные сессии, которые ломают восстановление контекста

**Файл:** `scripts_01/auto_conspect.py`, блок `if __name__ == "__main__":` (строки ~55-58).

Сейчас:
```python
sessions = cm.list_sessions(status=SessionStatus.ACTIVE)
if not sessions:
    print("No active sessions. Starting a test session...")
    snap = cm.start_session(project="freebuff", topic="Auto-conspect test")
    # ... добавляет тестовые сообщения, конспектирует, complete_session
```

Этот код задумывался как демо-пример использования, но его же исполняет
`scripts_01/cron_conspect.sh` каждые 30 минут (`*/30 * * * *` в crontab).
Результат в `data_13/context.db`: 19 из 60 сессий — это `"Auto-conspect test"`
с одинаковым `message_count=2`, созданные строго по расписанию крона, а не
реальной работой.

Проблема: `scripts_01/bootstrap.py` и `scripts_01/stream_bridge.py::_auto_bootstrap()`
берут `cm.list_sessions(SessionStatus.ACTIVE)` и `ORDER BY updated_at DESC`
(`scripts_01/context_manager.py::list_sessions`, строки ~617-636), то есть просто
самую свежую по времени. Если крон срабатывает позже, чем закончилась реальная
рабочая сессия, следующее восстановление контекста подхватывает фиктивную
"Auto-conspect test" вместо настоящей — отсюда ощущение "Buffy вспоминает не
последнюю задачу, а предпоследнюю/препредпоследнюю".

**Что сделать:**
1. Разделить `auto_conspect.py`: вынести demo/test-код в отдельный `scripts_01/_demo_auto_conspect.py` или под явный флаг `--demo`, никогда не исполняемый кроном.
2. В `if __name__ == "__main__":` реального `auto_conspect.py`: если `list_sessions(ACTIVE)` пуст — просто логировать `"Нет активных сессий, пропуск"` и выйти с кодом 0, ничего не создавая.
3. Написать одноразовый скрипт очистки: удалить из `data_13/context.db` все строки `sessions` с `topic='Auto-conspect test'` и связанные `messages`/`checkpoints` по `session_id`. Перед удалением — вывести count и попросить подтверждение.
4. Добавить регрессионный тест: `cron_conspect.sh`/`auto_conspect.py --cron` при пустом списке ACTIVE-сессий не создаёт новых записей в БД (assert `sessions_count_before == sessions_count_after`).

### Баг Б: TASK.md создан один раз и не обновляется, хотя правило это требует

**Факт (проверено через git):**
```
git log --follow -- TASK.md
4f5d063 feat: Freebuff/Buffy Project 2.0 — Agentic Platform & Knowledge OS
```
Один коммит с момента основания проекта. После него — минимум 15 функциональных
коммитов (MCP Server, FastAPI wrapper, security fixes, Model Gateway streaming,
Orchestrator+EventBus), а TASK.md всё ещё описывает статус 🟢 ГОТОВО по задаче
"Система стриминга контекста v2.0" как единственную и текущую.

Причина не концептуальная ("Buffy игнорирует правила") — правило в `docs_10/core/RULES.md`
сформулировано конкретно и однозначно. Причина механическая: нигде в коде нет
триггера, который заставил бы это правило исполниться:
- в `freebuff_cli.py` нет команды создания/обновления TASK.md (есть `start`, `status`, `resume`, `conspect`, `list`, `checkpoint`, `seed`, `buffy`, `qwen-resume` — задачи среди них нет);
- в `scripts_01/doc_reminder.sh::check_docs()` проверяются только `TROUBLESHOOTING.md`, `DECISIONS.md`, `REFERENCES.md`, `OVERLAY_IMPLEMENTATION.md` и `README.md` — TASK.md не проверяется вообще.

**Что сделать:**
1. Добавить в `freebuff_cli.py` команду `task start "<название>"`:
   - если текущий `TASK.md` имеет `Статус: 🟢 ГОТОВО` или `❌ ОТМЕНЕНО` — архивировать его в `docs_10/tasks_archive/TASK_<slug>_<date>.md` и создать новый из `docs_10/ops/TASK_TEMPLATE.md`;
   - если статус не финальный — предупредить: "Предыдущая задача не закрыта: <название>. Продолжить всё равно? (архивирует как есть)".
2. Добавить в `freebuff_cli.py status` вывод возраста TASK.md и его текущего статуса.
3. Добавить проверку TASK.md в `scripts_01/doc_reminder.sh::check_docs()` по тому же паттерну, что и для остальных обязательных файлов (age + статус).
4. В `scripts_01/bootstrap.py` при восстановлении сессии — если TASK.md старше N дней и не в статусе ГОТОВО, вывести предупреждение в стартовый промпт Buffy.

---

## Часть 2 — риск, найденный попутно (не главная тема, но нельзя игнорировать)

`tests_09/test_mcp_server.py` не проходит сборку:
```
NameError: name 'Optional' is not defined
```
(`Optional`/`Tuple` используются в аннотациях без `from typing import Optional, Tuple`).
Это значит модуль вообще не импортируется, а `.github/workflows/pytest.yml` гоняет
`pytest tests_09/ -v --tb=short` без `--ignore` — то есть CI по идее должен падать
целиком на каждом пуше. Проверь:
1. Действительно ли CI сейчас красный на GitHub Actions, или workflow не подключён/не запускается.
2. Почини импорт в `test_mcp_server.py`.
3. После починки — реально прогони `pytest tests_09/` и подтверди фактический count (для сверки: без этого файла у меня прошло 474 passed, 2 skipped).
4. Раз уж ты внутри — `docs_10/session_dumps/2026-07-28_mcp_server.md` заявляет "53 теста" для MCP-сервера. Проверь, что все они реально в файле и реально проходят после фикса импорта, и поправь цифру в SESSION_DUMP, если она разошлась.

---

## Часть 3 — твои 10 рекомендаций

Проанализируй систему целиком (BUFFY.md, RULES.md, все scripts_01/*.py, EventBus,
Orchestrator, MemoryEngine, KnowledgeEngine, MCP-серверы, тесты, CI) и дай
**10 конкретных рекомендаций по масштабированию/усилению системы**. Требования
к каждой рекомендации:

- Не абстрактная ("улучшить наблюдаемость"), а привязанная к конкретному файлу/механизму — что именно там не так и что добавить.
- С обоснованием: что сломается или не будет масштабироваться, если оставить как есть.
- С оценкой усилия (S/M/L) и приоритетом (High/Medium/Low).
- Если рекомендация пересекается с уже описанными в Части 1-2 багами — не дублируй, ссылайся.

Начни с этих направлений (это не готовые формулировки, а зоны, где стоит покопать
самому — цифры и детали подтверди по факту, не выдумывай):

1. Идемпотентность и защита от дублей во всех cron/scheduled-задачах (не только auto_conspect — проверь остальные `scripts_01/*.sh` и cron-паттерны на тот же класс бага).
2. Хранение секретов — `.keys/*.keys` как плоские файлы на диске вместо Vault-подобного механизма (Vault на 8400 порту уже есть — почему KeyPool его не использует?).
3. CI: помимо мёртвого теста из Части 2 — есть ли покрытие security-регрессий (`exec()`, `shell=True`) линтером/pre-commit, чтобы такие вещи не попадали в код повторно?
4. Feedback-loop для документации: `doc_reminder.sh` есть, но реагирует только по расписанию/вручную — можно ли повесить его как git pre-commit/post-commit хук, чтобы предупреждение всплывало в момент коммита, а не через час.
5. Дублирование данных о сессиях: SQLite (`context_manager.py`) + файловый стриминг (`stream_session.py`) + summaries в Markdown — три источника правды об одном и том же. Оцени, не расходятся ли они и не пора ли выбрать один source of truth.
6. Рост `context_12/checkpoints/` и `context_12/summaries/` — файлов уже больше сотни, есть ли retention/архивация, или они будут расти неограниченно.
7. `.mypy_cache`/`.pytest_cache` внутри `projects_17/tg_terminal_messenger` — попадают ли такие директории в архивы/бэкапы проекта по ошибке, и стоит ли ужесточить `.gitignore`/backup-скрипты.
8. EventBus и Orchestrator — судя по `docs_10/decisions/DECISIONS.md`, недавно добавлен parallel execution. Есть ли там race conditions вокруг `context.db` при параллельных пишущих сессиях (SQLite + многопоточность — частый источник багов).
9. Onboarding нового агента/инстанса Buffy в сессии — есть `docs_10/ops/AGENTS.md`, `BUFFY.md`, `docs_10/ops/SESSION_GUIDE.md` — нет ли рассинхронизации между ними (одно и то же правило описано по-разному в двух местах, и какое из них главное).
10. Метрика "здоровья" документации — `doc_reminder.sh` считает issues по факту наличия/возраста файлов, но не по смысловому расхождению (TASK.md может формально существовать и быть протухшим по содержанию, как сейчас). Стоит ли добавить более смысловую проверку (например, сверка последней даты в CHANGELOG.md с последним git-коммитом).

Для каждой рекомендации, которую подтвердишь фактами — покажи, что именно ты
проверил (grep/файл/коммит), не давай советов "на глаз".

---

## Формат ответа

1. Подтверждение выполнения Части 0 (секреты) — что сделано.
2. Патчи для Части 1 (баг А, баг Б) — diff или полный новый код файлов.
3. Результат проверки CI/теста из Части 2.
4. 10 рекомендаций из Части 3 — каждая с: файл/механизм → проблема → что сделать → эффорт → приоритет.
5. Итоговый список изменённых/созданных файлов.
```

Можешь отдать этот промт как есть. Если агент, который будет выполнять, не имеет доступа к этому чату — вложи вместе с промтом тот же архив `freebuff_tar.gz`, иначе он не сможет сослаться на конкретные строки и коммиты.