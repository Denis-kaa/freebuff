Прочитал документ целиком. Прежде чем составлять промт с твоими двумя замечаниями и своими — важно назвать то, что вижу сам, потому что здесь есть более серьёзная проблема, чем два отсутствующих раздела, и её нужно решить первой, иначе новый документ унаследует ту же болезнь.

## Главная проблема — этот документ сам нарушает то, что мы решили час назад

Раздел 9 целиком посвящён ребрендингу в LEVIATHAN — и там написано: "в более ранних документах уже встречается имя LEVIATHAN, и оно описывает амбицию лучше... для пользователя платформа будет называться LEVIATHAN". Но мы только что, в этом же разговоре, договорились с тобой на конкретном примере (отклик МИТЛИС): название рано использовать во внешней коммуникации, "это маркетинговое название, для него ещё рано". PLATFORM.md — это документ, который явно adresован внешнему читателю ("написан для человека, который только что посмотрел на платформу... можно объяснить соседу, жене, директору"). Значит агент, писавший этот файл, либо не знал о решении, которое мы приняли в разговоре, либо проигнорировал его. Это первое, что нужно поправить, прежде чем добавлять твои и мои замечания — иначе новый документ повторит ту же ошибку.

## Второе, что важно назвать прямо — характер расхождения между "что есть" и "что описано"

Смотрю на раздел 10 ("Как платформа уже работает") против того, что мы реально проверяли фактами в LESSONS.md и ARCHITECTURAL_DEBT.md несколько часов назад. Документ пишет "Multi-agent runtime" как нечто существующее в разделе 6 ("В коде платформы уже заложено это"), ссылаясь на `freebuff_plugin_03/mesh/` (Session Mesh v2.0) и `runtime_05/scenarios/19_remote_sync/` — но мы не проверяли эти конкретные модули, и они не всплывали ни в дампе, ни в LESSONS.md, которые я разбирал. Это ровно тот паттерн, который был у Bridge Layer/ACP Protocol в самом начале разговора — компонент называется "готовым", хотя проверка нужна отдельно.

Ещё показательнее — раздел 12, пункт 41: "Будет ли платформа уметь работать в команде? Несколько человек в одном workspace" — вынесен как **открытый вопрос**, хотя мы в основной части сессии уже подробно спроектировали Teamwork-режим (роли, DPE-концепция, переход Single→Cowork→Teamwork). Это значит, что агент, писавший PLATFORM.md, либо не видел SESSION_UNDERSTANDING документ, либо писал независимо от него — и как раз твои "single/cowork/team" — не просто "добавить раздел", это **уже решённая архитектура, которая почему-то не попала в документ вообще**.

Теперь пройдусь по твоим двум пунктам и добавлю свои — коротко, чтобы не растягивать перед промтом.

**Твои замечания, подтверждаю оба:**
1. Single/Cowork/Teamwork — отсутствуют полностью, хотя решены в SESSION_UNDERSTANDING (раздел 4.3 про режимы, раздел 8 про их отсрочку в реализации, но не в архитектуре).
2. "Как работают чаты" и "умные заметки" — документ вообще не описывает механику самого диалога с агентом (как выглядит одно сообщение, что происходит при отправке), только абстрактные сценарии "шаг 1, шаг 2".

**Мои добавления:**
3. Название LEVIATHAN использовано преждевременно, вопреки уже принятому решению.
4. Раздел 7 (Cross-Workspace Intelligence) — прямая копия идеи, которую мы разбирали и признавали недоказанной несколько часов назад (Obsidian/Notion как категориальная ошибка сравнения) — документ выдаёт это за "главную фишку платформы" без единой оговорки о непроверенности.
5. Каждое утверждение "это уже реализовано" в разделах 6, 8, 10 нужно перепроверить против реального кода, а не переписывать с чужих слов — часть источников (`freebuff_plugin_03/mesh/`) не всплывала ни в одном документе, который мы фактически проверяли.
6. Отсутствует раздел про Learning-режим и механизм переноса между режимами — тоже решённая архитектура (раздел 8 SESSION_UNDERSTANDING), которая должна была попасть в пользовательский документ.

```markdown
# MISSION: Переписать PLATFORM.md на основе проверенного кода и уже принятых решений

## Контекст

Приложи SESSION_UNDERSTANDING_2026-08-02.md к этой задаче — это протокол
решений, к которым мы пришли ранее. Текущий PLATFORM.md написан без опоры
на этот документ и содержит расхождения, которые нужно исправить, не просто
добавить недостающие разделы поверх старого текста.

**Формат сохранить** — простой язык, без жаргона, структура "для человека,
который спрашивает что это вообще такое". Но содержание должно измениться
существенно, не косметически.

---

## ЗАДАЧА 0 — Убрать преждевременное название

Раздел 9 текущего PLATFORM.md позиционирует "LEVIATHAN" как имя, готовое
к использованию с пользователями ("для пользователя платформа будет
называться LEVIATHAN"). Это противоречит уже принятому решению: название
рано выносить во внешнюю коммуникацию (см. метафору в конце
SESSION_UNDERSTANDING — "нельзя защищать урожай, которого ещё нет").

Убери упоминания LEVIATHAN как решённого будущего имени. Если хочешь оставить
факт, что ребрендинг рассматривается — one line, без развёрнутого раздела,
без "амбиция описана лучше". Платформа называется рабочим именем до тех пор,
пока явно не будет сказано иначе.

---

## ЗАДАЧА 1 — Проверить каждое "уже реализовано" против реального кода

Документ содержит утверждения вида "в коде уже заложено", ссылающиеся на:
- `freebuff_plugin_03/mesh/` (Session Mesh v2.0)
- `runtime_05/scenarios/19_remote_sync/`
- `core_02/remote_sync.py`, `core_02/telegram_contract.py`
- "больше 2000 автоматических тестов"

Прежде чем переносить любое из этих утверждений в новый документ — открой
файл, проверь, что он реально существует и делает то, что описано, а не
просто называется многообещающе. Если модуль существует, но не тестировался
на практике (в отличие от, например, ScenarioRegistry/SmartRouter, которые
уже прошли реальный стресс-тест на interior_planner) — пометь это явно как
"написан, но не проверен в деле", не как "работает".

Раздели новый документ на три честные категории по каждому упомянутому
компоненту:
- **Проверено на практике** (есть реальный прогон, реальный результат —
  как ScenarioRegistry+Wizard+SmartRouter на interior_planner, PB-7 найден
  и исправлен).
- **Написан, но не проверен** (код есть, тестов/прогона на реальной задаче
  нет).
- **Не существует, только идея** (упоминалось в разговорах/промтах, кода нет).

---

## ЗАДАЧА 2 — Добавить режимы Single/Cowork/Teamwork

Сейчас отсутствуют полностью. В SESSION_UNDERSTANDING (раздел 4.3 про
оркестратора и раздел 3 про режимы как мост) это уже решённая архитектурная
модель. Опиши простым языком:
- Single — один человек, один workspace, платформа как персистентный
  контекст поверх одного или нескольких инструментов.
- Cowork — один человек, несколько агентов/провайдеров одновременно.
- Teamwork — несколько людей, общий проект, ролевое разделение.

Явно укажи текущий статус каждого режима по факту (см. Задачу 1) — Single
частично проверен на практике, Cowork и Teamwork — архитектура спроектирована,
но раздел 8 SESSION_UNDERSTANDING прямо откладывает их реализацию (не
разрабатывать сейчас, это осознанное решение, не забытая часть).

Не выдавай Teamwork за существующую функцию — но и не убирай её из документа,
как было раньше (пункт 41 открытых вопросов противоречил уже принятому
решению).

---

## ЗАДАЧА 3 — Описать механику диалога и умных заметок

Текущий документ описывает только сценарий "первого знакомства" (шаги 1-5)
и абстрактные "слои памяти". Не хватает:
- Как выглядит обычное сообщение пользователя агенту — что происходит
  между отправкой и ответом (какие слои памяти реально задействуются,
  не абстрактно "пять слоёв", а на примере одного конкретного сообщения).
- Умные заметки — идея из разговора: пользователь набрасывает мысль
  без привязки к конкретному проекту, агент сам определяет, к какому
  проекту/workspace это относится, и раскладывает. Если это уже
  архитектурно заложено (проверь) — опиши как есть. Если это пока
  только идея без кода — пометь явно как нереализованное, не выдавай
  за готовую функцию.

---

## ЗАДАЧА 4 — Смягчить категоричность Cross-Workspace раздела

Раздел 7 текущего документа ("ядро, которое проглядывает") называет
cross-workspace анализ "главной фишкой платформы" без оговорок. Проверь
факт: сравнение с существующими инструментами (Obsidian с графом связей,
Notion AI) не проводилось — эта идея не проверена на уникальность.

Перепиши раздел с honest framing: это интересное направление, архитектурно
возможное (укажи, что конкретно уже есть — если что-то из перечисленного
в разделе 7 текущего документа реально существует после проверки по
Задаче 1), но не "главная фишка", доказанная практикой — это гипотеза,
ждущая проверки на реальном использовании.

---

## ЗАДАЧА 5 — Добавить Learning-режим

Отсутствует полностью, хотя решён в SESSION_UNDERSTANDING (дополнение
к разделу 8 про режимы: не отдельная образовательная платформа, а режим
объяснения происходящей работы, включаемый по кнопке, с явным разовым
переносом артефактов между режимами — не автоматической синхронизацией).
Опиши коротко, тем же простым языком, с тем же честным статусом
"идея, архитектурно совместима, не реализована".

---

## ФОРМАТ ОТВЕТА

Новый PLATFORM.md той же структуры (для непрограммиста, без жаргона), но:
1. Без преждевременного названия LEVIATHAN как решённого факта.
2. С честной трёхуровневой пометкой по каждому компоненту (проверено /
   написано-не-проверено / только идея) — не одним словом "уже работает"
   для всего подряд.
3. С разделами Single/Cowork/Teamwork и Learning, отражающими то, что
   реально решено в SESSION_UNDERSTANDING, а не заново придуманными.
4. С описанием механики одного сообщения и умных заметок.
5. С открытыми вопросами, не противоречащими уже принятым решениям —
   если вопрос уже решён в SESSION_UNDERSTANDING, убери его из списка
   открытых, перенеси ответ в основной текст.

Не пиши код на этом шаге — это работа с документом и с фактической
проверкой существующего кода на предмет "что оно реально делает", не
разработка новой функциональности.
```

Отдавай вместе с файлом SESSION_UNDERSTANDING_2026-08-02.md — без него агент не увидит, что Single/Cowork/Teamwork и Learning уже решены, и снова напишет их с нуля или пропустит.