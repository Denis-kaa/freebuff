Ты — ведущий инженер-агент. Подключись к моей платформе и действуй строго в рамках её методологии, изучив её перед началом работ.

## ТВОЯ КЛЮЧЕВАЯ ЦЕЛЬ
Разработать, интегрировать и запустить модуль "Диспетчер моделей" (Model Dispatcher).
Суть модуля: Автоматизация работы с установленным инструментом FreeBuff через терминал.
Задачи модуля:
- Находить и запускать FreeBuff.
- Управлять сессиями (перезапуск, продление, обработка вылетов).
- Брать промты из входной папки.
- Отправлять их в нужную LLM (DeepSeek, GLM, MiniMax).
- Сохранять ответы в выходную папку.

## ЭТАП 1: ИССЛЕДОВАНИЕ ПЛАТФОРМЫ (ОБЯЗАТЕЛЬНО)
1. Изучи корневую структуру проекта, чтобы понять, как устроена платформа.
2. Найди файлы, описывающие правила ведения проектов (методологии, шаблоны, каноны, контракты).
3. Ты ОБЯЗАН следовать этим правилам. Если находишь расхождения — фиксируй их в журнале.
4. Не придумывай свою структуру документации, если она уже регламентирована платформой.

## ЭТАП 2: АУДИТ ТЕКУЩЕГО СОСТОЯНИЯ
1. Проверь содержимое рабочей папки: `/storage/emulated/0/PROJECTS/workstation/freebuff/`
2. Определи, есть ли там:
   - Старые попытки реализации (нерабочие, частично рабочие).
   - Готовые скрипты или конфиги.
   - Файлы документации проекта.
3. Классифицируй всё найденное. Реши, что делаем:
   - Удаляем как мусор (если это действительно мусор, а не артефакт).
   - Оставляем как основу.
   - Рефакторим (переписываем логику, но сохраняем идею).
   - Переносим в правильные папки, если они лежат не по канонам.

## ЭТАП 3: ИНИЦИАЦИЯ ИЛИ РЕАНИМАЦИЯ ПРОЕКТА
1. Если в папке уже есть признаки проекта (Паспорт, Roadmap), прочитай и обнови их.
2. Если проекта нет — создай его с нуля по правилам платформы:
   - Создай Паспорт Проекта.
   - Создай Дорожную карту (Roadmap).
   - Создай журнал выполненных шагов.
   - Создай журнал уроков и ошибок.
   - Создай файл архитектурных решений (ADR), если требуется.

## ЭТАП 4: ПРОЕКТИРОВАНИЕ И РАЗРАБОТКА
1. Составь технический план: какие скрипты, классы, функции нужны.
2. Определи, как именно скрипт будет запускать FreeBuff:
   - Если FreeBuff уже есть в PATH — используем его.
   - Если FreeBuzz лежит где-то рядом — используем абсолютный/относительный путь.
   - Если его нет — предусмотри вывод ошибки и создай файл настроек (например, config.yaml или .env).
3. Напиши код (Python 3). Требования:
   - Модульность (разделение на логические файлы/классы).
   - Чистый ввод-вывод через subprocess.
   - Перехват ошибок (try-except, timeouts).
   - Чтение промтов из входной папки.
   - Запись ответов в выходную папку.
   - Логирование шагов и ошибок.
   - Строгая изоляция: работаем только внутри `/storage/emulated/0/PROJECTS/workstation/freebuff/`, не лезем в системные папки.
4. Настрой ротацию моделей: если одна модель дала сбой или зависла, скрипт должен пробовать следующую из списка.

## ЭТАП 5: ТЕСТИРОВАНИЕ
1. Создай тестовый промт-файл.
2. Прогони свой код хотя бы логически (если нет возможности выполнить — опиши, что должно произойти при запуске).
3. Убедись, что пути к папкам верные для Termux.

## ЭТАП 6: ФИНАЛИЗАЦИЯ
1. Заполни Roadmap: отметь, что сделано, что в планах.
2. Заполни Журнал шагов: подробно, что делал.
3. Если сталкивался с проблемами — запиши уроки.
4. Сформируй краткий отчет для пользователя: что создано, как запустить, что настроить.

## ОГРАНИЧЕНИЯ
- Работай в Termux (Android).
- Код должен работать на Python 3.10+.
- Если чего-то не знаешь (например, точную команду запуска FreeBuff) — оставь заглушку и напиши комментарий в коде `# TODO`.
- Не жди подтверждения на каждом шаге, действуй последовательно до конца.
- Комментарии и документация — на русском.

## ФОРМАТ ОТВЕТА
В конце выдай:
1. Что было сделано (список изменений).
2. Структура файлов в итоге.
3. Как запустить модуль.
4. Какие параметры нужно заполнить вручную (если не удалось определить автоматически).
5. Рекомендации по развитию проекта дальше.

Приступай немедленно.