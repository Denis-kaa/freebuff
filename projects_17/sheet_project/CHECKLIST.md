# CHECKLIST — sheet_project

## Pre-flight (перед каждым прогоном)

### Окружение
- [ ***REMOVED*** Python: `python3 --version` (>= 3.10)
- [ ***REMOVED*** openpyxl: `python3 -c "import openpyxl; print(openpyxl.__version__)"` (>= 3.1.0)

### Зависимости
- [ ***REMOVED*** `pip install -r ../../requirements.txt` (без ошибок)

### Тесты
- [ ***REMOVED*** `python3 -m pytest tests/ -q` (зелёные)

### Артефакты
- [ ***REMOVED*** RUNNABLE.md, CHECKLIST.md, README.md присутствуют
