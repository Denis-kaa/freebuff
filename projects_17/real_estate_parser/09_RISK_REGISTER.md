# 09_RISK_REGISTER — риски

| Риск | Вероятность | Влияние | Митигация |
|---|---|---|---|
| Источник-портал под Cloudflare/anti-bot | Высокая | Блокировка парсинга | TLSClient-абстракция + optional curl_cffi; выбор источников с SSR/API |
| Источник стал SPA (урок W-16: Kwork) | Средняя | Парсер перестаёт работать | Живая проверка каждого источника перед написанием адаптера; `_looks_like_spa_shell()`-диагностика |
| Смена HTML-структуры источника | Средняя | Поломка парсера | Валидатор отбраковывает неполные записи + алерт в боте /errors |
| Rate limit / бан по IP | Средняя | Прокси горят | Bounded concurrency + per-host rate limit + retry с jitter + circuit breaker |
| Юридический (ToS, robots.txt) | Средняя | Претензии | Policy-гейт: парсинг только публичных карточек, без обхода авторизации, уважительный интервал |
| Python 3.14.6/Termux: нет wheel для curl_cffi | Средняя | Нет TLS-impersonation локально | Fallback на httpx; curl_cffi — только на сервере деплоя |
| Секреты в коде/репо | Низкая | Утечка | Только env-переменные; .env в .gitignore; секреты через человека (C-категория) |
| Прокси-провайдер недоступен/дорог | Средняя | Нет ротации | Провайдер-агностичная реализация (URL-список в env); SQLite-режим работает без прокси для локальных тестов |
