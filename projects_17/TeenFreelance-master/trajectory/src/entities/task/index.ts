/**
 * Entity: task — a freelancer's assignment inside a project,
 * including the versioned review loop.
 * Canonical shapes live in src/types.
 */
export type { Task, TaskStatus, TaskVersion } from '../../types';
