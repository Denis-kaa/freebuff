/**
 * Feature: team-builder («Драфт», concept Часть 1 §3 / СИСТЕМНЫЙ ПРОМТ §4 Этап 3.2).
 *
 * Phase 2 will implement:
 *   - <TeamBuilder /> — filter freelancers by skill combination;
 *   - useDraftSystem() — assemble a project team from the talent pool.
 *
 * Stage 1 intentionally ships the contract surface only (see @entities/user,
 * @entities/project) so upper layers can be typed before UI exists.
 */
export {};
