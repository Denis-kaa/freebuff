/**
 * Feature: team-builder («Драфт», concept Часть 1 §3 / СИСТЕМНЫЙ ПРОМТ §4 Этап 3.2).
 *
 * Implemented in Phase 4a (2026-09-06):
 *   - draft state + scoring live in `app/store.ts` (shared between views);
 *   - UI lives in `widgets/team-builder` (skill picker → mentor gate → roster).
 *
 * This feature layer stays the contract surface for future domain logic
 * (invite notifications, real project briefs) — no shape redefinitions here.
 */
export {};
