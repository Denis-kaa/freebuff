/**
 * Widget: team-builder — the «Драфт» (concept Часть 1 §3, Этап 3.2).
 * Skill picker + min-level filter → scored candidates → mentor gate →
 * team roster → project creation.
 */
export { TeamBuilder } from './index.tsx';
