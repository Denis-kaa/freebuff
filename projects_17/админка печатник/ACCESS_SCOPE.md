# ACCESS_SCOPE.md — границы доступа для задачи «Калькуляторы»

> Статус: COMPLETE · Режим работы: **A (READ ONLY) → B (PREPARE, изолированная копия)** · Дата: 2026-09-07
> Источник правил: `SERVER_ACCESS_WHIMCO.md` (прочитан полностью) + `AGENTS.md` (границы B1–B14, Server-first ADR-022).

## 1. Разрешённые ресурсы

| Ресурс | Что разрешено |
|---|---|
| Локальный файл `SERVER_ACCESS_WHIMCO.md` | чтение |
| Локальный архив `projects_17/админка печатник/Калькуляторы.zip` | чтение, проверка целостности; **изменение запрещено** |
| Сервер whimco (SSH-алиас `whimco`, root, ключ из конфига Termux) | только команды, разрешённые правилами: проверка живости, чтение структуры, копирование в разрешённый каталог |
| Рабочий каталог проекта на сервере | `/opt/freebuff/projects_17/админка печатник/` (создан в рамках задачи; не существовал ранее — проверено) |
| Репозиторий `/opt/freebuff` | **не изменялся**; HEAD на момент старта: `4a2b87c` |

## 2. Разрешённые операции (выполнены в рамках правил)

- проверка доступности сервера (`echo OK; hostname; git rev-parse`);
- проверка архива: `stat`, `sha256sum`, `unzip -t` (только чтение);
- копирование архива на сервер в разрешённый каталог (`scp`);
- распаковка **копии** архива в `worktree/` (оригинал на телефоне не тронут);
- статический аудит: `find`, `grep`, `python3 -c ast/json` (только чтение);
- неразрушающие launch-пробы: `timeout 5s python3 <entry>` в изолированной копии (без установки пакетов).

## 3. Запрещённые операции (не выполнялись и не будут без подтверждения)

- удаление/перезапись чего-либо в `/opt/freebuff` вне каталога задачи;
- изменение `data_13/context.db`, `context_12/events.db`, `.env`;
- установка системных пакетов (`tkinter`, `Pillow`, `pywebview`, `reportlab` — не устанавливались);
- изменение production-сервисов, cron, firewall, SSL, пользователей;
- изменение исходного кода калькуляторов (до завершения аудита — правило §5 промта);
- публикация/деплой чего-либо.

## 4. Действия, требующие отдельного подтверждения (будущие этапы APPLY)

Любая реализация целевого приложения: изменение кода в `worktree/`, установка зависимостей на сервер, запуск длительных сервисов, перенос в production. Формат запроса — по §10 промта («ТРЕБУЕТСЯ ПОДТВЕРЖДЕНИЕ…»).

## 5. Резервное копирование и rollback

- Серверный бэкап платформы существует: `/opt/freebuff-server-backup-20260829T201345Z` (проверен `stat`).
- Все артефакты задачи изолированы в `/opt/freebuff/projects_17/админка печатник/`; rollback = удаление этого каталога (только по явному подтверждению пользователя).
- Оригинальный архив на телефоне — неизменяемый источник (§6 промта): не копировался поверх, не переименовывался.

## 6. Секреты

- Сканов на credentials в исходниках не выполнялось сверх grep-паттернов аудита; найденные совпадения (`subprocess`-вызовы) не содержат секретов.
- В отчётах токены/пароли/ключи не фигурируют (§11 промта соблюдено).

## 7. DoD этапа

- [x] файл правил прочитан полностью;
- [x] границы доступа определены;
- [x] разрешённый сервер определён (whimco, SSH-алиас);
- [x] разрешённый каталог определён (`/opt/freebuff/projects_17/админка печатник/`);
- [x] правила подтверждения определены (§10 промта);
- [x] опасные действия перечислены (§3, §4);
- [x] ACCESS_SCOPE.md создан;
- [x] неизвестных критических ограничений нет.
