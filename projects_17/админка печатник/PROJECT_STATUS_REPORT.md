# PROJECT_STATUS_REPORT.md — полный отчёт по этапам и соответствие кода

> Дата: 2026-09-10 · Версия на сервере: `0d67152` (мастер = GitHub = сервер)
> Состояние: **249 тестов зелёные, mypy clean (50 файлов), сервис активен, БД чистая**
> Код: `printcalc/` — 10 229 строк Python (движок 2 653 + web-слой 5 109 + тесты), 22 тестовых модуля, 11 шаблонов, 12 отчётов

---

## 1. Сводка «что сделано» (13 этапов)

| # | Этап | Коммит | Дата | Ядро результата |
|---|------|--------|------|-----------------|
| 0 | Аудит реализации | `1badef4` | 09-08 | IMPLEMENTATION_AUDIT_REPORT по 15 пунктам ТЗ |
| 1 | Клиенты + материалы | `2a85211` | 09-08 | clients/contacts/materials, matching, реестр |
| 2 | Сметы + snapshot | `53578a8` | 09-09 | Estimate, immutable snapshot (checksums), заказ из сметы |
| 3 | Расход материалов | `5f32d63` | 09-09 | consumption engine → заказы (расход по движку) |
| 3b+4 | Листы + производство | `7377a88` | 09-09 | SHEET_NESTING, каталог операций, задания, QC-гард |
| 5 | Склад | `7d6196c` | 09-09 | ledger движений, резерв/списание, план закупок |
| 5b | Склад×Производство | `aa2ff49` | 09-09 | автосписание при закрытии последнего задания |
| 6 | Communication Hub | `feee1c9`+`0786c0e` | 09-09 | Unified Inbox, парсер, client-matching, TG-поллер |
| 7 | Порт Digital | `3b9d364` | 09-10 | калькулятор цифровой печати, 9 golden-тестов |
| 8 | Кассовые услуги P0 | `337076f` | 09-10 | сид 20 услуг, CSV-шаблон round-trip |
| 9 | R5+R10 | `db18281` | 09-10 | подсказки min_order, needs_operator+reasons |
| 10 | Вывески + парсер v2 | `f13649a` | 09-10 | порт sign, 6 golden, мультизаказ-парсер |
| 11 | Исследование | `69bc030` | 09-10 | RESEARCH_ADOPTION_PLAN: калибровка цен, R1–R10 |

---

## 2. Отчёт по каждому этапу

### Этап 0 — Аудит (старт работ)
**Коммит:** `1badef4` · **Отчёт:** IMPLEMENTATION_AUDIT_REPORT.md

Проверка 15 пунктов ТЗ (промт_4 §2) перед началом кода: окружение, схема БД, движок, канонические материалы Wide. Результат — зелёная база, все пункты «в наличии».

### Этап 1 — Клиенты и материалы
**Коммит:** `2a85211` · **Файлы:** `store.py` (clients/contacts/materials), `api.py` (/clients, /materials), `clients.js`, `materials.js`, `db.py`

- Клиент ≠ контакт: у клиента список контактов (телефон/@ник/email), поиск по всем.
- Реестр материалов: рулонные (ширина рулона, ₽/м²) и листовые.
- Сид 10 канонических материалов Wide (баннер/плёнка/холст и т.д.) при старте.
- Выбор клиента в карточке заказа на главном экране.

### Этап 2 — Сметы и snapshot
**Коммит:** `53578a8` · **Файлы:** `store.py` (estimates/estimate_items/calc_snapshots), `estimates.js`, `templates/estimates.html`

- Смета — первоклассная сущность; позиции резолвит **сервер** тем же механизмом, что заказы (цена клиенту не доверяется).
- **Immutable snapshot** (§49): в calc_snapshots пишутся checksums engine/registry/catalog — воспроизводимость расчёта гарантирована.
- Статусы сметы; «заказ из сметы» — одной кнопкой.

### Этап 3 — Расход материалов в заказах
**Коммит:** `5f32d63` · **Файлы:** `api.py` (/consumption/calculate), `store.py` (consumption_json), движок `engine/consumption/`

- Правило владельца: **поворот изделия разрешён** — если ширина (3000 мм) не входит в рулон, движок сам разворачивает: длина↔ширина.
- **Ручная ширина рулона** в диалоге расчёта (подсказка из реестра материалов) — на случай смены плоттера на 3 м.
- Расход хранится в order_items.consumption_json — база для склада.

### Этап 3b+4 — Листовые материалы и производство
**Коммит:** `7377a88` · **Файлы:** движок consumption (SHEET_NESTING, 6 тестов), `store.py` (operations/production_tasks), `production.js`, `templates/production.html`

- SHEET_NESTING: лист 3000×2000, полистный раскрой с поворотом, расход табличек.
- Каталог операций (12 операций-сид с триггерами) → генерация заданий по правилам (монтаж последним, ламинация только с флагом, идемпотентно).
- Жизненный цикл задания pending/in_progress/done/blocked; **QC-гард**: без полного чек-листа задание не закрыть.
- Кнопка «В производство» в карточке заказа.

### Этап 5 — Склад
**Коммит:** `7d6196c` · **Файлы:** `store.py` (stock_movements, reserve/consume/release/adjust), план закупок, склад-панель в materials.js

- Ledger `stock_movements` — **закрытый словарь** PURCHASE/RESERVE/RELEASE/CONSUME/ADJUST (ANTI-6b: никаких «типов на ходу»).
- Резерв/списание заказа **без ручного ввода** — из consumption_json (идемпотентно, RESERVE→CONSUME конвертация, release после consume блокирован).
- ADJUST — set-семантика инвентаризации.
- План закупок: Need = Required − Available − Reserved, округление вверх до pack_size.

### Этап 5b — Автосписание
**Коммит:** `aa2ff49` · **Файлы:** `store.py` (complete_task → auto-consume), `production.js` (уведомление)

- При завершении **последнего** задания заказа резерв автоматически конвертируется в списание.
- Работает и **без резерва**, если расход рассчитан движком (§20 MASTER: «материалы списываются»).
- Никогда не блокирует завершение задания (ошибки склада гасятся), идемпотентно (повтор не спишет дважды).

### Этап 6 — Communication Hub
**Коммиты:** `feee1c9`, `0786c0e` (graceful degradation) · **Файлы:** `db.py` (inbox_messages/inquiries), `store.py` (ingest/lifecycle/matching), `telegram.py` (поллер), `inbox.js`, `templates/inbox.html`

- Идемпотентный приём по (channel, external_id): дубль → created=false.
- Детерминированный парсер текста (без LLM — §37): словарь каталога + числа.
- Client matching по chat_id/@нику/телефону: none/matched/manual.
- Жизненный цикл: сообщение → заявка (new) → estimated (смета прикреплена).
- TG-поллер long polling в daemon-потоке; **без токена — no-op, приложение стартует** (урок деплоя: httpx сделан опциональным).

### Этап 7 — Порт Digital
**Коммит:** `3b9d364` · **Файлы:** `calculators/digital/{config,spec,compute}.py`, `tests/test_digital_golden.py`

- Порт 1:1 `calc_digital.py` (строки 335–455): плотность раскладки с зазором 2 мм, прайс по диапазонам тиража, duplex-математика, мин. заказ 500 ₽, наценка 30%, налог 6% (канон платформы).
- **9 golden-тестов** с независимой деривацией: визитка 100 шт = **563.83 ₽** — живой расчёт на сервере совпал до копейки.

### Этап 8 — Кассовые услуги P0
**Коммит:** `337076f` · **Файлы:** `p0_services.py` (каталог из исследования), `store.py` (seed_p0_services, template CSV, import-template), `api.py`, `price_list.js`

- Сид **20 услуг в 7 разделах** (1. КОПИРОВАНИЕ … 7. ПРОЧЕЕ) с синонимами и unverified=0.
- CSV-шаблон «скачал → поправил цену в Excel → загрузил» (round-trip проверен живую: 15→18→15 ₽).
- Парсер: стемминг флексий («ксерокса» → «ксерокс») и число ПЕРЕД услугой («2 ксерокса» → qty 2).

### Этап 9 — R5 + R10
**Коммит:** `db18281` · **Файлы:** `calculators.py` (_min_order_hints), `app.js` (#calc-hint), `parser.py` (_operator_signals), `inbox.js`

- **R5**: подсказка «Минимальная сумма заказа: 500 ₽ / Минимальный тираж: 500 шт» в диалоге расчёта ДО расчёта — из канонических конфигов, ничего не дублируется.
- **R10**: парсер возвращает `needs_operator` + `reasons` (нераспознанные токены, файл/макет, несколько позиций) — на «Входящих» плашка «⚠ передать оператору». Не молча.

### Этап 10 — Вывески + парсер v2
**Коммит:** `f13649a` · **Файлы:** `calculators/sign/{config,spec,compute}.py`, `tests/test_sign_golden.py`, `parser.py` (v2), отчёт PHASE_SIGN_PARSERV2_REPORT.md

- **Вывески** (порт sign_calc.py, 1270 строк): 5 моделей (буквы/короб), геометрия букв `w = h·0.7`, материалы «Авто» + фолбэк, подсветка (короб 900 ₽/м², буквы по периметру 0.6·450), монтаж по высоте (3500 + 0/10/20/30%), электрика/доставка/подложка/каркас/блоки, **два режима цены** (прайс за единицу ИЛИ себестоимость+наценка 25%).
- 6 golden-тестов: буква 40 см = **539.63 ₽**, полный короб = **18 305.85 ₽** — живой расчёт совпал.
- **Парсер v2 (Блок C, 05_combined_orders.md)**: сегментация «и/плюс/ещё» → segment_id; число после перечисления относится к следующей услуге («ксерокс 5 и фото 20» → две позиции с qty 5 и 20); unassigned_fragments (не съедаем фрагменты); volume_hints («от 101 шт — дешевле») из диапазонов Digital.

---

## 3. Соответствие кода канону платформы

| Правило (AGENTS.md/CODE_QUALITY) | Как выполнено в коде | Где проверить |
|---|---|---|
| **ANTI-6b: закрытый словарь** | Статусы движения склада — frozenset-подобный кортеж; опции STRING-полей калькуляторов резолвятся ТОЛЬКО из конфигов (`_OPTION_RESOLVERS`); подсказки R5 — из конфигов, не дублируются | `store.py: INQUIRY_STATUSES`, `calculators.py: _OPTION_RESOLVERS/_min_order_hints` |
| **Single Source of Truth цен** | Цену клиенту не доверяем: заказ/смета пересчитывают цены сервером (`_resolve_item`); расчётные позиции — только через реестр движка | `store.py`, `api.py:/calculate` |
| **Идемпотентность** | Приём сообщений (channel, external_id); attach_estimate; резерв/списание; генерация заданий; сид P0/материалов (повтор — no-op) | `store.py` везде, тесты `test_inbox.py`, `test_stock.py` |
| **Additive Architecture** | Все миграции — ALTER ADD COLUMN / CREATE IF NOT EXISTS; новые поля парсера (segment_id, volume_hints) — аддитивно к контракту | `db.py: _migrate`, `parser.py: parse()` |
| **Contract First** | CalcResult/CostLine — единый контракт движка; все 5 калькуляторов ему следуют; golden-тесты закрепляют паритет с legacy | `engine/result.py`, `test_*_golden.py` |
| **Observability** | Журнал этапов в РОАДМАП_v6 (12 записей с коммитами), 12 PHASE_*.md отчётов, живые смоук-проверки после каждого деплоя | `РОАДМАП_v6_ЕДИНАЯ_СИСТЕМА.md`, отчёты |
| **Backward Compatibility** | Контракты API не ломались: `items/unknown/needs_operator/reasons` сохранены; дубликаты калькуляторов в реестре ловятся ошибкой | тесты `test_api_web.py` (прошли без правок контракта) |
| **Не молча (анти-галлюцинация)** | unknown-токены возвращаются; needs_operator+reasons; unassigned_fragments; жаргон не выдумывается (словарь только из каталога+спек) | `parser.py` |
| **Server-first (ADR-022)** | Каждый этап: коммит → push в GitHub → pull на whimco → рестарт → живой смоук → чистка смоук-данных | история деплоев, лог роадмапа |

**Верификация на текущий момент:** `249 passed`, mypy clean (50 файлов), сервис `printcalc-web` active на `0d67152`, БД чистая: 20 позиций прайса, 10 материалов, 0 смоук-остатков (заказы/сметы/сообщения/движения — по нулям).

---

## 4. Дальнейшие шаги (по приоритету)

### Ближайшие (следующая сессия)

1. **Портировать ЧПУ-калькулятор** (`cnc_calc.py`, 29 КБ из архива) — фрезерная/лазерная резка. Тот же конвейер, что Digital/Вывески: config → spec → compute → golden-тесты с деривацией → реестр/парсер.
2. **UI-обвязка мультизаказа**: карточка заказа из нескольких сегментов парсера (сегмент → позиция) — парсер уже отдаёт segment_id, осталась форма.
3. **Портировать Дизайн** (`design_calc.py`, 43 КБ) и **Себестоимость** (`universal_calc.py`, 55 КБ) — закрывают матрицу калькуляторов.

### Этап 6b — Communication Hub v2

4. **Email-канал** (EmailAdapter §43) + **ответы из системы** (reply из «Входящих» с шаблонами R1/R2/R3).
5. **Активация Telegram-бота** — нужен токен от Дениса: `PRINTCALC_TG_TOKEN` (+ `PRINTCALC_TG_ADMIN`) в env юнита и рестарт; код готов и ждёт.

### Этап 7 — Payment Hub

6. Оплата заказов: TestAdapter → TBank (промт_4).

### Организационное (вопросы Денису — RESEARCH_ADOPTION_PLAN §6)

7. **Ценовые решения**: визитки 2 ₽/шт (рынок 7–15), мин. заказ Digital 500 vs 700 ₽, шаг люверсов 50 vs 30 см, простой монтаж от 1200 ₽? Ответы = правка CSV-шаблона прайса прямо в Excel (round-trip уже работает).
8. **Приём файлов** (Блок E): files[] в заказе, валидатор формата/DPI, warning «не печатаем Word» — зависит от решения по хранению файлов.

---

*Отчёт составлен: 2026-09-10 · Путь: `projects_17/админка печатник/PROJECT_STATUS_REPORT.md`*
