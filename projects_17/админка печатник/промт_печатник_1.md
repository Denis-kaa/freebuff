Понял — я не выбираю из списка, я сам должен определить, чего мне не хватает для проектирования полной архитектуры, и сформулировать, что нужно выяснить у агента (у того, что имеет доступ к серверу/коду/WorkflowForms), чтобы закрыть пробелы.

Разложу это как чёткое ТЗ для агента — вопросы, сгруппированные по темам, от которых реально зависят архитектурные решения.

Понял направление. Существующий план описывает классический сценарий "каталог → выбрать калькулятор → форма → результат" — это **не то**, что вы хотите. Вы хотите обратный поток: "клиент говорит пожелания → система сама ведёт диалог/парсит текст → сама определяет какие услуги и запускает нужные калькуляторы". Это новый слой, которого архитектура пока не покрывает.

Собрал вопросы к агенту, сгруппированные по темам — от них зависят конкретные архитектурные решения:

Так, важно: план явно откладывает "Смета → Заказ (заявка в WF-стиле)" в **V2**, а импорт цен из Excel в V1. То есть настоящей интеграции с реальным WorkflowForms (запись в его БД/формат) там нет — это скорее заимствование идей, а не реальный экспорт. Мне нужно это прояснить, прежде чем проектировать.

Вот блок вопросов агенту — сформулирую его так, чтобы вы могли прямо переслать:

---

**Вопросы к агенту (нужны для проектирования слоя "быстрый ввод заказа")**

**1. Про сам WorkflowForms — что значит "экспорт в WF"**
- WorkflowForms сейчас реально используется в работе типографии, или это лежит в архиве как наследие? Если используется — как именно вы сейчас заводите туда заказ вручную (через какую форму, сколько полей)?
- У WF есть какой-то доступный интерфейс для записи данных извне: импорт файла (Excel/XML), прямой доступ к MS SQL базе, HTTP API, или единственный способ — руками через десктоп-клиент?
- Проверь: реально ли развёрнут сервер WorkflowEngine и MS SQL, к которым можно подключиться, или это гипотетическая (Г) часть аудита, а по факту WF просто стоит на одном компьютере как изолированный клиент?
- Если прямого доступа нет — устроит ли Дениса такой вариант: генерировать файл в формате, который WF умеет импортировать (например, тот самый Excel-шаблон CopyCentre), без прямой записи в БД?

**2. Про диалоговый чек-лист / скрипт вопросов**
- Есть ли у Дениса уже готовый список типовых вопросов, которые он задаёт клиенту при заказе (для баннера, визиток, вывесок и т.д.), или это нужно собрать заново по каждому из 8 калькуляторов на основе их input_schema?
- Подтверди по каждому из 8 модулей (M1–M8): какие поля — обязательные для расчёта, а какие можно доспросить после (или взять по умолчанию)? Это определяет, сколько вопросов будет в мастере для каждой услуги.

**3. Про "свободный ввод → авто-распознавание нескольких услуг"**
- Нужно понять реалистичный сценарий: клиент диктует "баннер, визитки и меню" голосом/текстом, и Денис одной строкой вбивает это — как именно должно выглядеть распознавание: по ключевым словам (простой словарь: "баннер"→M4, "визитки"→M1 и т.д.), или это нужно через LLM-парсинг свободного текста?
- Если ключевые слова — нужен явный маппинг: список синонимов/жаргона, которым пользуются реальные клиенты и сам Денис (например "визитка" vs "визитки" vs "визка"), чтобы не гадать.

**4. Про порядок и приоритет модулей**
- В MVP уже выбраны Riso/Tablichki/Wide/Digital. Совпадает ли это с тем, что Денис реально считает чаще всего для новых заказов, или приоритет надо пересмотреть под сценарий "быстрый ввод"?