# РОАДМАП_v7_SMART_ORDER.md — Умный технологический разбор заказа + Reports Hub

> **Статус:** ACTIVE — порядок потоков A→B утверждён Денисом (2026-09-12) · **Дата:** 2026-09-12
> **Основа:** промт_печатник_6.md (Smart Order Intelligence, фазы R0–R5) ·
> reports-hub-spec.md (сервис отчётов, §14 порядок) · PROJECT_STATUS_REPORT.md (13 этапов, parser v2).
> **Принципы:** ANTI-5 (один сценарий за раз) · каждый этап = модель + API + тесты + UI + отчёт +
> коммит (§62 промт_4) · Additive Architecture · LLM на слое правил не участвует.

---

## 0. Два потока и рекомендуемый порядок

| Поток | Что это | Спека |
|---|---|---|
| **A — Smart Order Intelligence** | Детерминированный слой правил поверх parser v2: нормализация → проверка → подсказки | промт_печатник_6.md |
| **B — Reports Hub** | Сервис красивых Apple-style отчётов (`services_08/reports_hub/`) | reports-hub-spec.md |

**Рекомендация: A → B.** Почему:

1. Правила — узкое место производства: без них сотрудник вручную переводит «порезать поштучно»
   в операции — ровно то, что промт_4 §9 запрещает. Это прямая ценность продукта.
2. Отчёты этапов A (`PHASE_RULES_R*.md`) будут созданы как обычные .md — Reports Hub
   подхватит их **ретроспективно** через автообход (doc library сканирует все .md).
   Строить хаб раньше — значит строить витрину для пустой библиотеки.
3. ANTI-5: не вести два больших сценария одновременно.

Переключение потоков (например, B → A) — только явным решением Дениса, фиксируется здесь в журнале.

---

## 1. Этап S0 — Схема YAML-паков правил + инвентаризация (1–2 дня) 🔲

*(= PHASE R0 промт_6 + незавершённый дизайн схемы паков)*

1. **REGISTER-FIRST:** `python -m core_02.missing_registry register order_rules_engine --kind module`
   (+ `order_rules_engine.api`, `suggestions_ui`) — до кода.
2. **Спроектировать схему пака** — см. Приложение A (черновик готов, утвердить на примерах
   sticker и banner). Валидатор схемы: drift словарей → `ValueError` (ANTI-6b).
3. **Инвентаризация правил** из документации: OPERATIONS_CATALOG.md, BUSINESS_RULES.md,
   QUESTION_FLOW.md, UPSELL_RULES.md, CALCULATOR_MATRIX.md. Правила, которых нет в документации —
   UNKNOWN → список вопросов Денису (не придумывать).
4. **Выход:** `engine/rules/packs/sticker.yaml` + `banner.yaml` (v1, 15–30 правил),
   таблица правил, `PHASE_RULES_R0_REPORT.md`.

**DoD:** валидатор проходит на двух паках; каждый `propose_operation` существует в каталоге
операций (12 сид-операций); список открытых вопросов зафиксирован в отчёте.

## 2. Этап S1 — Нормализация (PHASE R1, 2–3 дня) 🔲

- `engine/rules/normalize.py`: ParseResult → `OrderDraft` (маппинг жаргона на токены словаря).
- Golden-тесты: 4 примера интервью + 6 примеров PROJECT_STATUS_REPORT.
- **DoD:** факты нормализуются однозначно; неизвестное не теряется (unknown/needs_operator).

## 3. Этап S2 — Движок правил (PHASE R2, 2–3 дня) 🔲

- `engine/rules/engine.py`: ProductionRule → `RuleVerdict` (proposed/missing/suggestions).
- `missing` из Spec.required калькулятора + require правил.
- Анти-тест: правило с токеном вне словаря → ValueError. Идемпотентность вердикта.
- **DoD:** `ready_for_calculator` корректен на всех golden-примерах (бэклит без количества → False).

## 4. Этап S3 — API (PHASE R3, 1–2 дня) 🔲

- `POST /api/order/analyze` → RuleVerdict JSON (аддитивно; ручной ввод идёт через тот же endpoint).
- **DoD:** живой смоук на whimco: «Наклейка 50×30, 149 шт + резка по контуру» → вердикт с
  предложением «Плоттерная резка» и вопросом про макет.

## 5. Этап S4 — UI подсказок (PHASE R4, 2–3 дня) 🔲

- Блоки «Распознано» → «Предложения» (✓/Изменить) → «Нужно уточнить» (Да/Нет/Уточнить позже).
- Ничего не применяется без действия сотрудника; подтверждения логируются (кто/когда).
- Mobile-first, единый стиль приложения.
- **DoD:** сценарий «порезать поштучно» проходится от ввода до заказа без знания терминологии.

## 6. Этап S5 — Связка с калькулятором и заданиями (PHASE R5, 1–2 дня) 🔲

- `ready_for_calculator=true` → авто-переход в расчёт без повторного ввода.
- Подтверждённые операции → существующий конвейер заданий (идемпотентно).
- **DoD:** MVP-цикл §60 промт_4: текст → разбор → уточнения → смета → задание.

## 7. Поток B — Reports Hub (после потока A; спека §14) 🔲

| Шаг | Что | Тесты спеки |
|---|---|---|
| H1 | Каркас `services_08/reports_hub/` + ReportModel + автообход projects_17 | 3, 5 |
| H2 | Extract-слой: markdown, metrics, gitstats | 1, 2 |
| H3 | Рендер + Apple-токены + главная + отчёт печатника | 6, 9 (golden) |
| H4 | Diff между генерациями + summaries.json (агент заполняет) | 4, 8 |
| H5 | Сервер (token-гейт, zip) + отчёт платформы | 7 |
| H6 | systemd-юнит `reports-hub.service`, порт 8310, деплой + смоук | — |

Примечание: к моменту H3 в библиотеке доков печатника уже будут лежать все
`PHASE_RULES_R0–R5` — golden-фикстура станет богаче и реалистичнее.

## 8. Каденция и правила ведения (для каждого этапа)

1. Один этап = одна сессия (ANTI-5). Этап начат → доведён до отчёта.
2. После каждого этапа: `python -m pytest tests_09/ -q` + mypy → commit → push →
   pull на whimco → рестарт → живой смоук → чистка смоук-данных (SYNC_RUNBOOK, CON-69).
3. Отчёт этапа — `PHASE_RULES_R<n>_REPORT.md` по образцу существующих PHASE-отчётов
   (шапка Статус/Дата, таблица файлов, метрики, открытые вопросы) — формат, пригодный
   для Reports Hub без переделки.
4. Журнал этапов ведётся здесь (§9); PROJECT_STATUS_REPORT.md обновляется по завершении потока A.

## 9. Журнал этапов

**Записи решений:**

- **2026-09-12 · УТВЕРЖДЕНИЕ ПОРЯДКА A→B** — Денис утвердил рекомендуемый порядок §0:
  сначала поток A (Smart Order Intelligence, S0–S5), затем поток B (Reports Hub, H1–H6).
  Основание — §0 (правила — узкое место производства; отчёты потоков подхватятся хабом
  ретроспективно; ANTI-5 — один сценарий за раз). Переключение потоков ранее завершения A —
  только новым решением Дениса с записью здесь.
- **2026-09-12 · ДЕПЛОЙ S0 (`86eb680`)** — коммит → push в GitHub (d071ecd..86eb680) →
  рестарт printcalc-web на whimco → живой смоук: сервис active, HTTP 200, паки sticker/banner
  валидируются под прод-venv (PyYAML 6.0.3 добавлен в /opt/printcalc-venv; extras `rules`
  объявлен в pyproject). Примечание: data_13/missing_registry.yaml — в .gitignore по политике
  репо, реестр живёт серверно-локально (фиксация в коммит-сообщении и отчёте R0 §5).

| Этап | Статус | Дата | Коммит | Отчёт |
|---|---|---|---|---|
| S0 | ✅ завершён и задеплоен | 2026-09-12 | 86eb680 | PHASE_RULES_R0_REPORT.md |
| S1–S5 | 🔲 не начаты | — | — | — |
| H1–H6 | 🔲 не начаты | — | — | — |

## 10. Параллельные решения Дениса (не блокируют, но влияют)

- TG-токен (`PRINTCALC_TG_TOKEN`) — код ждёт, активация отдельной строкой.
- Ценовые решения (визитки 2 ₽/шт, минзаказ 500 vs 700, шаг люверсов 50 vs 30 см,
  монтаж от 1200 ₽) — правка CSV-прайса; шаг люверсов влияет на правило BANNER_GROMMETS_STEP.
- Правила, требующие ответа: «нужна ли подготовка макета как платная операция?», «крепление
  табличек ПВХ — какие варианты в каталоге?», «фото на документы — отдельный калькулятор или P0-услуга?».

---

## Приложение A — черновик схемы YAML-пака ProductionRule

**Файл пака:** `engine/rules/packs/<product>.yaml` (pack = продукт = закрытый словарь,
зеркалит реестр калькуляторов).

```yaml
# engine/rules/packs/sticker.yaml
schema_version: 1
pack: sticker
product: sticker                      # ОБЯЗАТЕЛЬНО: токен закрытого словаря продуктов
rules:
  - rule_id: STICKER_CUT_CONTOUR_PLOTTER      # уникален глобально (все паки)
    when_facts:                               # v1: только точное равенство, значения — строки
      cutting: "true"
      cutting_mode: "contour"
    propose_operation: plotter_cut            # ТОЛЬКО токен каталога операций | null
    require:                                  # токены закрытого набора MissingItem.field
      - quantity
      - layout_with_cut_contour
    suggest:
      - kind: question                        # закрытый набор: question|finishing|material|upsell|layout
        label: "Макет: есть готовый макет с контуром резки?"
        options: ["Да, макет готов", "Нужно подготовить макет", "Макета нет"]  # ≤ 4, строки
        reason: "sticker + cut_contour → нужен контур в макете (QUESTION_FLOW §…)"
      - kind: material
        label: "Материал: самоклеящаяся плёнка (по умолчанию из реестра)"
        reason: "sticker → материал по умолчанию (MATERIAL_MODEL)"
```

```yaml
# engine/rules/packs/banner.yaml
schema_version: 1
pack: banner
product: banner
rules:
  - rule_id: BANNER_GROMMETS_STEP_CONFIRM
    when_facts:
      grommets: "true"
    propose_operation: grommets_install       # если есть в каталоге операций; иначе null + вопрос
    require: [quantity]
    suggest:
      - kind: question
        label: "Шаг люверсов: 30 см?"
        options: ["Да, 30 см", "Другой шаг"]
        reason: "факт из текста подтверждаем; канон: шаг 50 vs 30 см — вопрос Денису"
  - rule_id: BANNER_MONTAGE_OFFER             # БИЗНЕС-ПРАВИЛО: предложить, НЕ добавить (промт_6 §1)
    when_facts:
      grommets: "true"
    propose_operation: null
    suggest:
      - kind: upsell
        label: "Монтаж: нужен монтаж?"
        options: ["Да", "Нет", "Уточнить позже"]
        reason: "баннер + люверсы → вероятно уличный (UPSELL_RULES)"
```

**Правила валидации схемы (validator при старте, drift → ValueError):**

1. Обязательные поля пака: `schema_version`, `pack`, `product`, непустой `rules[]`;
   `pack == product`; `schema_version == 1`.
2. `rule_id` — уникален глобально (по всем пакам), формат `ПРОДУКТ_СУТЬ`.
3. `propose_operation` — токен существует в каталоге операций (`store.py`) или `null`.
4. Токены `require` — из закрытого набора полей MissingItem (quantity, size_mm, layout_with_cut_contour, …).
5. `suggest[].kind` — из закрытого кортежа; `options` — 1–4 строки; `reason` — обязателен
   (никакой магии: у каждой подсказки видно правило-источник).
6. Правило обязано иметь хотя бы `propose_operation` или непустой `suggest` — иначе это не правило.
7. `when_facts` — только equality в v1 (детерминизм; диапазоны/альтернативы — отдельная
   версия схемы через REGISTER-FIRST).
8. `when_product` наследуется от пака; перекрытие в правиле запрещено (однозначность).

---

*РОАДМАП_v7 · Поток A (Smart Order S0–S5) → Поток B (Reports Hub H1–H6). Утверждение порядка — за Денисом.*
