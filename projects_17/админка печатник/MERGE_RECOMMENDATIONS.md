# MERGE_RECOMMENDATIONS.md — 10 рекомендаций по объединению

> Статус: COMPLETE · Дата: 2026-09-07 · Ровно 10, без дублирования.

| № | Рекомендация | Причина | Эффект | Риск без неё | Приоритет | Сложность | Переиспользуемое |
|---|---|---|---|---|---|---|---|
| 1 | **Единый движок калькуляторов** (контракт: metadata + input schema + calculate + result schema) | сейчас 8 несовместимых реализаций (T-01) | новый калькулятор = 1 модуль + схема | рост дублирования ×N при каждой фиче | **Critical** | M | формулы из `calculate()` каждого модуля |
| 2 | **Выделить расчёты в pure-функции** (без Tk/JS) | логика вшита в GUI (T-05) | unit-тесты, переиспользование в web и EXE | любая правка цены непроверяема | **Critical** | M | тела `calculate()` Riso/Tablichki/Sign/Wide/Digital/Costing |
| 3 | **Единый реестр справочников** (материалы, прайсы, оборудование, налог, мин. заказ) | 3+ разных JSON и хардкод `TAX_RATE=0.06` (T-10) | одно место правки цен | расхождение цен между модулями (уже есть: люверсы) | **Critical** | M | settings.json, cnc_settings.json, equipment.json |
| 4 | **Унификация UI на web-стеке** (FastAPI + веб-фронт; webview-обёртка для desktop) | 2 стека (Tk + webview) (U-01) | один UX, мобильный доступ | двойная поддержка навсегда | High | L | HTML/JS из design_calc/cnc_calc; палитра |
| 5 | **Регрессионные тесты расчётов** (golden-наборы: вход → цена) | тестов 0 (T-06) | защита цен от регрессий | молчаливые ошибки маржи | High | S | существующие формулы как ожидаемые значения |
| 6 | **Выбрать канонические версии модулей** (wrapper-копии) и убрать дубли | 4 модуля в 2 редакциях (T-02) | единая логика | баги в одной копии не попадают в другую | High | S | diff-анализ уже выполнен |
| 7 | **requirements.txt + фиксированные версии** | зависимостей нет (T-03) | воспроизводимый запуск/сборка | сборка EXE невоспроизводима | High | S | список импортов аудита |
| 8 | **Заменить bare `except:` на конкретные + лог** | 151 bare except, 74 pass (T-04) | видимые ошибки сохранения | потеря настроек молча | Medium | M | существующие try-блоки |
| 9 | **Единый экспорт: CSV + PDF для всех калькуляторов** | PDF только в Макетах, CSV без PDF в остальных | клиент получает документ везде | ручное перепечатывание смет | Medium | M | ReportLab-код design_calc.py:115-192 |
| 10 | **Чистка поставки** (build/, dist/, __pycache__, пустой `python`, print-дебаг) | 96 мусорных файлов, 326 МБ (T-08, T-09, U-06) | лёгкий репозиторий | путаница «какая версия рабочая» | Medium | S | — |

## DoD

- [x] ровно 10; [x] каждая с обоснованием; [x] измеримый эффект; [x] приоритет; [x] сложность (S/M/L); [x] без дублирования.
